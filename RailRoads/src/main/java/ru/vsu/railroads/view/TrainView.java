package ru.vsu.railroads.view;

public class TrainView {
    /*private TrainService trainService = new TrainService();

    public <T> void createItem(String curClass, Map<String, String> params) {
        if(trainService.create(curClass, params))
            System.out.println("Successfully created and added "+curClass);
        else
            System.out.println("Failed to create "+curClass);
    }

    public <T> void readItems(Class<T> curClass) {
        for (T item : trainService.readAll(curClass))
            System.out.println(item.toString());
    }

    public <T> void readItemByID(Class<T> curClass, Long id) {
        //System.out.println((trainService.readByID(curClass, id)).toString());
    }

    public <T> void updateItem(Class<T> curClass, String[] params) {
    }

    public <T> void deleteItem(Class<T> curClass, Long id) {
    }*/
}
